package observerproxy;

//TODO: Should have a private predicate which is passed when instantiating it and a private integer to count every time the operation with this predicate is executed

public class QueryObserver extends Observer{

    @Override
    public void update(int newState) {
        // do something
    }
}
